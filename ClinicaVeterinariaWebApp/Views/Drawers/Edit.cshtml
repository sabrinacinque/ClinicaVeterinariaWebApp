@model ClinicaVeterinariaWebApp.Models.Drawer

@{
    ViewData["Title"] = "Edit Drawer";
}

<h1>Edit Drawer</h1>

<h4>Drawer</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Number" class="control-label"></label>
                <input asp-for="Number" class="form-control" />
                <span asp-validation-for="Number" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CabinetId" class="control-label">Cabinet Code</label>
                <select asp-for="CabinetId" class="form-control" asp-items="ViewBag.Cabinets"></select>
                <span asp-validation-for="CabinetId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
